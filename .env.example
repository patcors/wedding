# Copy to .env and fill in the deployed Google Apps Script Web App URL.
# Anything prefixed with PUBLIC_ is exposed to the browser (Astro convention).
PUBLIC_RSVP_ENDPOINT=https://script.google.com/macros/s/AKfycb.../exec
